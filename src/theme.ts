import { ThemeConfig, ThemeMapping } from './index.config';

const light: ThemeConfig = {
  navBar: {
    bgColor: '#3d5a80',
  },
  header: {
    bgColor: '#98c1d9',
  },
};

export const themeMapping: ThemeMapping = { light };
