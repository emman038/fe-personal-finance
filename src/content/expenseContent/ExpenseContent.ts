import { EntryContent } from 'src/index.config';

const ExpenseContent: EntryContent = {
  heading: 'Expense',
  graphContent: {
    graphTitle: 'Expense Breakdown',
    graphLabels: ['TOTAL EXPENSE'],
  },
};

export { ExpenseContent };
