import { EntryContent } from 'src/index.config';

const IncomeContent: EntryContent = {
  heading: 'Income',
  graphContent: {
    graphTitle: 'Income Breakdown',
    graphLabels: ['TOTAL INCOME'],
  },
};

export { IncomeContent };
